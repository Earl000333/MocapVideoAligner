# MocapVideoAligner_0811 重构需求：新增「动捕-触觉对齐」页

## 0. 硬约束（最高优先级）

1. **现有「视觉-动捕对齐」功能一行都不能改**。包括其 UI、算法、
   `*_aligned.bvh` / `*_alignment.json` / `*_aligned_curves.csv` /
   `*_calibration.png` / `results_csv/*.csv` 的字段与写出逻辑。
2. 新功能必须以**新增 Tab 页 + 新增模块**的方式实现。若需要复用现有
   代码，只能「读」不能「改」；确需修改公共函数时，改为新增重载版本，
   保留原函数签名与行为。
3. 提交前必须回归验证：走一遍原视觉-动捕流程，导出物与改造前逐字节
   一致（`*_calibration.png` 除外）。

## 1. 功能目标

在现有 GUI 中新增一个页签「动捕-触觉对齐」，用于求解动捕与压力数据
之间的时间偏移量 `delta_t2`，交互形态**完全参照**现有视觉-动捕页。

映射关系：

| 现有视觉-动捕页 | 新增动捕-触觉页 |
| --- | --- |
| 4 路相机 cam1~cam4 | 2 路压力：`left` / `right` |
| 各相机运动能量曲线 | 各脚压力总和曲线 |
| 综合视觉能量曲线 | 左右脚压力总和之和 |
| BVH 运动能量曲线 | BVH 足底贴地程度曲线 |
| `delta_t` | `delta_t2` |

## 2. 数据输入

1. **BVH**：复用现有 BVH 加载器。**必须使用已经过视觉-动捕对齐后的
   BVH**（即 `*_position_aligned.bvh`），不是原始 BVH。页面上要有明确
   提示，并在加载时校验文件名后缀含 `_aligned`，否则弹警告。
2. **压力**：读取 PressureWasher 清洗产物 `contact_curve.csv`，
   列为 `time_s, left_sum, right_sum`，已重采样到统一参考 FPS。
3. **粗偏移锚点**：读取采集器 `meta.json` 的 `epoch_monotonic_us` 与
   `started_at_iso`，计算时钟粗偏移 `t_coarse`。**该值仅用于限定搜索
   窗口，不作为最终结果**。若 `meta.json` 缺失，搜索窗口退化为全局。

## 3. 曲线定义

### 3.1 动捕侧：足底贴地程度曲线

对左右脚分别计算：

1. 从 BVH 解析出该脚踝关节及脚部末端关节的世界坐标。
2. 取该脚所有相关关节 Z 坐标（按 `axis_preset` 决定竖直轴）的最小值，
   得到逐帧「足底最低点高度」`h(t)`。
3. 曲线值 `mocap_curve = -h(t)`，再做 min-max 归一化到 [0, 1]。
   含义：值越大表示脚越贴近地面。
4. 重采样到 `reference_visual_fps`，与压力曲线时间轴一致。

### 3.2 触觉侧：压力总和曲线

1. 直接取 `contact_curve.csv` 的 `left_sum` / `right_sum`。
2. min-max 归一化到 [0, 1]。
3. 不做任何时域平滑滤波（会抹掉着地冲击峰值）。

## 4. 对齐算法

1. 以 `t_coarse` 为中心，取前后各 ±200 ms 作为搜索窗口
   （窗口宽度做成可调参数，默认 200 ms）。
2. 在窗口内对 `mocap_curve` 与 `pressure_curve` 做归一化互相关，
   **左右脚各算一次**，得到 `delta_t2_left` / `delta_t2_right`。
3. 最终 `delta_t2` 取两者均值。若两者差值 > 50 ms，UI 标红警告，
   提示该 session 存在异常，需人工介入。
4. 同时记录两侧的**互相关峰值高度** `peak_left` / `peak_right`。
   峰值低于阈值（默认 0.3）时标红，表示对齐不可信。
5. 提供人工微调：滑块 + 数值输入框，可在自动结果基础上手动调整
   `delta_t2`，交互方式与现有页的 `delta_t` 微调完全一致。

## 5. UI 要求

参考视觉UI，设计，页面功能上是相同的
但是视频显示区域变成触觉显示区域，其中左右两脚的触觉的UI参考，add\Gait-Data-Collector

其中触觉有两个脚，因此曲线实际上是两个脚的曲线

## 6. 导出产物（也是参考当前的视觉-动捕对齐的导出）

**另开一套文件，不得写入或修改现有 `*_alignment.json`。**

输出到 `sync/output/<session_id>/`：

```text
<session_id>_pressure_alignment.json
<session_id>_pressure_aligned_curves.csv
<session_id>_pressure_calibration.png
```

### 6.1 `*_pressure_alignment.json`

| 字段 | 含义 |
| --- | --- |
| `session_id` | 会话编号 |
| `delta_t2` | 最终采用的动捕-触觉偏移量，单位秒 |
| `delta_t2_left` / `delta_t2_right` | 左右脚各自互相关结果 |
| `peak_left` / `peak_right` | 左右脚互相关峰值高度 |
| `t_coarse` | 时钟换算得到的粗偏移 |
| `search_window_ms` | 实际使用的搜索窗口宽度 |
| `manual_adjusted` | 是否经过人工微调，布尔 |
| `reference_fps` | 统一参考帧率 |
| `mocap_source_file` | 使用的已对齐 BVH 文件名 |
| `pressure_source_file` | 使用的 contact_curve.csv 路径 |
| `axis_preset` | 竖直轴预设 |
| `exported_at` | 导出时间 |

### 6.2 `*_pressure_aligned_curves.csv`

表头固定：

```csv
time_s,mocap_left,mocap_right,pressure_left,pressure_right
```

写入应用 `delta_t2` 平移后的四条归一化曲线。若某脚数据缺失，该列留空。

### 6.3 `*_pressure_calibration.png`

当前页面曲线图区域的截图，写法参照现有 `*_calibration.png`。

## 7. 验收标准

1. 原视觉-动捕流程回归通过，导出物与改造前一致。
2. 新页面能在测试 session 上跑出 `delta_t2`，左右脚差值在 50 ms 内。
3. 三个新文件均能正确生成，字段齐全，可被 pandas / json 直接读取。
4. `meta.json` 缺失、`contact_curve.csv` 缺列、BVH 未对齐这三种异常
   输入均有明确提示，不崩溃。